#!/bin/bash

#=================================================
# IMPORT GENERIC HELPERS
#=================================================

source /usr/share/yunohost/helpers

#=================================================
# No custom config panel for Calagopus Panel.
# All settings are managed through the Panel's own Admin UI.
# This script exists as a placeholder for YunoHost conventions.
#=================================================

ynh_app_config_run "$1"
